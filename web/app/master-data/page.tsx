import Link from "next/link";
import { AppShell } from "@/app/components/AppShell";
import { PageHeader } from "@/app/components/PageHeader";
const records = [["Profil Guru", "Identitas dan data profesional", "/master-data/guru", "Lengkap"], ["Profil Madrasah", "Identitas institusi dan aset dokumen", "/master-data/madrasah", "Lengkap"], ["Tahun Pelajaran", "Tahun aktif, semester, dan beban belajar", "#", "Belum diisi"], ["Mata Pelajaran & Kelas", "Penugasan mengajar dan peserta didik", "#", "Belum diisi"]];
export default function MasterDataPage() { return <AppShell><PageHeader eyebrow="MASTER DATA" title="Satu sumber data untuk semua perangkat" description="Lengkapi data pokok sebelum masuk ke tahap perencanaan." /><section className="record-list">{records.map(([title, detail, href, status]) => <Link href={href} key={title} className="record-row"><div><h2>{title}</h2><p>{detail}</p></div><span className={status === "Lengkap" ? "status success" : "status"}>{status}</span><b>→</b></Link>)}</section></AppShell>; }
